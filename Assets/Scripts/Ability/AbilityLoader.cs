using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AbilityLoader : MonoBehaviour
{
    public static void LoadAbility(IEnumerable<bool> ability_set, FighterCondition fighterCondition, Movement movement, Attack attack, Receiver receiver)
    {
        ;
    }
}
